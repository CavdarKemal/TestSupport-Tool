<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2 http://maven.apache.org/xsd/assembly-1.1.2.xsd">

    <id>distribution</id>

    <formats>
        <format>zip</format>
    </formats>

    <dependencySets>
        <dependencySet>
            <outputDirectory>/lib</outputDirectory>
        </dependencySet>
    </dependencySets>

    <fileSets>
        <!-- ABE/ENE/GEE-config.properties liegen im GUI-Modul selbst. -->
        <fileSet>
            <directory>${project.basedir}</directory>
            <includes>
                <include>ABE-config.properties</include>
                <include>ENE-config.properties</include>
                <include>GEE-config.properties</include>
            </includes>
            <outputDirectory>/</outputDirectory>
        </fileSet>

        <!-- log4j.properties liegt im Parent-Root. -->
        <fileSet>
            <directory>${project.basedir}/..</directory>
            <includes>
                <include>log4j.properties</include>
            </includes>
            <outputDirectory>/</outputDirectory>
        </fileSet>

        <!-- Entpackte ITSQ-Testfälle — Pendant zum assembly-Setup in
             testsupport_client/TestSupportGUI/src/assembly/distribution.xml. -->
        <fileSet>
            <directory>${itsq_testfaelle.unpacked-dir}</directory>
            <outputDirectory>/X-TESTS/ITSQ</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
        </fileSet>
    </fileSets>

</assembly>
